+++
version = 2
id = "verification.growth.composable-mcp"
kind = "verification"
method = "automatic"
target = "test/run.sh"
+++

# Verify composable MCP boundary

Run `./test/run.sh`. The scenario exercises only the standalone MCP server and its ordinary JSONL data directory; no OwlKnowledge, Owlspec, HTTP, or product-specific CLI is required.
