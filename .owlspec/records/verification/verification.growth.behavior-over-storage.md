+++
version = 2
id = "verification.growth.behavior-over-storage"
kind = "verification"
method = "automatic"
target = "test/run.sh"
+++

# Verify behavior over storage volume

Run `./test/run.sh`. The observable assertions concern external result counts, recommendations, and review decisions rather than record quantity.
